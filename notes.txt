scheduler will use algorithm classes to make decisions about which process should be assigned to the cpu

scheduler will record the time the running time of the current running process

use case
    fcfsAlcorithm.getNextProcess(self.readyQueue)

    the fcfsalgorithm class will store the cucrent running process 




!!!!!!!!!       algorithm.shoudlChangeProcess()
nextProcess = algorithm.getNextProcess(self.readyQueue) on the update method

if (nextProcess) // if not null
    self.currentRunningProcess = nextProcess